"use client";
import type { PaperItem } from "@/components/today/paper-card";
export function ReadingPaperDetail(_props: { paper: PaperItem; index: number; onClose: () => void }) {
  return null; // replaced in the paper-detail task
}
